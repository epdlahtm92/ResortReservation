<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.resort.mapper.ReviewMapper">
	
	<!-- Create -->
		<insert id="createOneReview" parameterType="com.resort.domain.Review">
			insert into review (reviewid, reviewtitle, reviewcontent, reviewuser) values (review_seq.nextval, '${Review.reviewTitle}', '${Review.reviewContent}', '${Review.reviewUser}')		
		</insert>
	
	<!-- Read -->
		<select id="readAllReview" resultType="com.resort.domain.Review">
			select * from review order by reviewid
		</select>
	
</mapper>